# テスト確認表（固定）

最終更新: 2026-03-12
対象: DX22_Project Debug|x64 / Release|x64

## 運用ルール
- 機能追加ごとに本表へケース追加 or 既存ケース更新を行う。
- 結果は `OK / NG / 未実施` で記録する。
- `NG` の場合は再現手順と修正方針を備考へ追記する。

## 確認ケース（必須）
| ID | 観点 | 手順（要約） | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|
| T01 | 当たり判定（AABB統一） | `SCENE_GAME`でプレイヤー/敵の接触、攻撃/敵、敵弾/プレイヤーの判定を確認。デバッグ線を確認。 | 判定はAABB基準で動作し、接触時の挙動（押し合い/判定可視化）が成立する。 | OK | Player/EnemyはAABB重なり解消、Attack/Enemyと敵弾/Playerは`HitAabb`で確認。 |
| T02 | 1スイング多段ヒット防止 | 1体の敵に対してF攻撃1回を当てる。 | 同一スイングで同一敵への重複ダメージが発生しない。 | OK | `lastHitSwingId != m_attackSwingId` 条件でコード確認。 |
| T03 | 敵全滅でWave進行 | Wave中の敵を全撃破。 | 最終Wave前なら `currentWave` が+1され、次Waveの敵が構成される。 | OK | `m_enemies.empty()` かつ `m_currentWave < m_waveMax` の分岐を確認。 |
| T04 | 最終Wave全滅でWin遷移 | 最終Waveの敵を全撃破。 | `SCENE_RESULT`へ遷移し、勝利表示になる。 | OK | 最終Wave分岐で `ResultType::Win` + `SCENE_RESULT` を確認。 |
| T05 | プレイヤーHP0でLose遷移 | 敵攻撃を受けてHPを0にする。 | `SCENE_RESULT`へ遷移し、敗北表示になる。 | OK | `hp <= 0` 分岐で `ResultType::Lose` + `SCENE_RESULT` を確認。 |
| T06 | BGM再生（Game/Result） | Game開始とResult遷移を行う。 | Gameで`GameBGM.mp3`、Resultで`ResultBGM.mp3`がループ再生される。 | OK | 各シーンで `LoadSound(..., true)` と `PlaySound` をコード確認。 |
| T07 | 攻撃SE再生 | F攻撃を敵にヒットさせる。 | ヒット時に`SE/attack.mp3`が再生される。 | OK | ヒット時に `PlaySound(m_pAttackSe)` 呼び出しを確認。 |
| T08 | 敵数変更時の再構成 | ImGuiで`enemyCount`を増減。 | 値変更に応じて敵数が再構成され、監視値も追従する。 | OK | `waveEnemyTarget != m_requestedEnemyCount` で `EnsureEnemyCount` 実行を確認。 |
| T12 | ボスBGM切替 | `waveMax >= 2`でプレイし最終Waveへ到達。 | 最終Wave到達時に`GameBGM2.mp3`へ切り替わる。 | OK | `m_currentWave >= m_waveMax` で `PlaySound(m_pBossBgm)` を確認。 |
| T13 | SEフォルダ分離 | `Assets/Sound/SE` 配下のSEを使って戦闘を行う。 | `attack/player_hit/enemy_attack/clear` が `SE` 配下から再生される。 | OK | `SceneGame` のSEロードパスが `Assets/Sound/SE/*.mp3` であることを確認。 |
| T14 | 遠距離型の敵弾攻撃 | 遠距離型を接敵させ、予兆後の攻撃を確認。 | 予兆後に敵弾が生成され、接触時のみ被弾する。回避中は被弾しない。 | OK | `SceneGame`で遠距離型のみ敵弾生成、敵弾AABB判定、`isPlayerEvading`時無効をコード確認。 |
| T15 | 勝利条件の一本化 | 最終Wave前に敵を残した状態で進行し、Goal未使用構成で勝利遷移しないことを確認。 | 最終Wave全滅時のみ `SCENE_RESULT(Win)` になる。 | OK | `SceneGame` にGoal生成/接触判定がなく、勝利は `m_enemies.empty()` 最終Wave分岐のみであることを確認。 |
| T16 | Win時の強化三択 | 勝利後のResultで `1/2/3` を押下。 | 候補1つが適用され、`SCENE_GAME` に戻る。 | OK | `selectionPending` 中に `ApplyUpgradeSelection` 成功で `SCENE_GAME` 遷移をコード確認。 |
| T17 | Win時リロール | 勝利後のResultで `R` を押下（残回数あり）。 | 候補が再抽選され、残回数が減る。0時は再抽選不可。 | OK | `RerollUpgradeSelection` の `selectionPending`/残回数条件をコード確認。 |

## 追加ケース（今回の優先7）
| ID | 観点 | 手順（要約） | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|
| T09 | 設定自動読込 | `Assets/gameplay_tuning.cfg` を編集して起動。 | 起動時に編集値が `Gameplay` タブへ反映される。既定運用ではミラー候補からも読込できる。 | OK | `Transfer` のフォールバック読込（`Assets` / `x64/Debug/Assets` / `DX22_Project/Assets` / `../../DX22_Project/Assets`）を確認。 |
| T10 | 設定自動保存 | `Gameplay`値変更後に終了→再起動。 | 終了時保存され、再起動後に値が保持される。既定運用ではミラー候補へ同期保存される。 | OK | `Transfer` の既定保存が複数パス同期（4系統）であることを確認。 |
| T11 | 手動保存/読込ボタン | ImGuiの`設定を保存`/`設定を読込`を押下。 | ファイルへ保存・ファイルから反映が行われる。 | OK | ボタンが `Transfer::Save/LoadGameplayTuning` を直接呼ぶことを確認。 |

## 追加ケース（2026-02-27 / タイトル・リザルト・ポーズUI改修）
| ID | 観点 | 手順（要約） | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|
| T18 | タイトル選択導線 | タイトルで `Start / Option / Exit` を `WASD/矢印` で移動し、`Enter/F/Space` で決定。 | 入力系が統一され、選択移動と決定が正常動作する。 | OK | 2/27改修の手動確認結果を反映。 |
| T19 | タイトルEsc終了 | タイトルで `Esc` を押下。 | アプリ終了導線が動作する。 | OK | 2/27改修の手動確認結果を反映。 |
| T20 | ポーズ表示/停止 | `SCENE_GAME` 中に `Esc` でポーズ表示。 | ポーズ中にゲームプレイ更新が停止し、メニュー操作のみ有効。 | OK | 2/27改修の手動確認結果を反映。 |
| T21 | ポーズ選択導線 | ポーズの `続行 / Option / Titleに戻る` を `WASD/矢印 + Enter/F/Space` で操作。 | タイトルと同一操作系で選択/決定できる。 | OK | 2/27改修の手動確認結果を反映。 |
| T22 | リザルト選択導線 | リザルトの `Restart / Title` を `WASD/矢印 + Enter/F/Space` で操作。 | 選択表示と遷移が正常動作する。 | OK | 2/27改修の手動確認結果を反映。 |
| T23 | Option(Title/Pause)音量操作 | Title/PauseのOptionで `Master/BGM/SE` を変更。 | 音量変更が反映される。 | OK | ポーズ中変更の即時反映も確認済み。 |
| T24 | Option(Title/Pause)画面モード切替 | Optionで `Fullscreen / Window` を切替。 | 排他状態で切替できる。 | OK | 2/27改修の手動確認結果を反映。 |
| T25 | Option Close操作 | Option右上 `Close` を押下。 | OptionWindowが閉じる。 | OK | 2/27改修の手動確認結果を反映。 |

## 追加ケース（2026-03-02 / 床差し替え・難易度UI画像化・Release表示対応）
| ID | 観点 | 手順（要約） | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|
| T26 | Title操作ガイドのRelease表示 | `Release|x64` で起動し、タイトル右下の操作ガイドを確認。 | Debug専用分岐に依存せず、Releaseでも操作ガイドが表示される。 | OK | 3/2改修の手動確認結果を反映。Debug側の二重表示なし前提で確認済み。 |
| T27 | Win強化選択UIのRelease表示 | `Release|x64` で通常ステージ勝利後、強化選択画面を確認。 | `SCENE_RESULT` のWin時に、候補あり/なし両方でDebugと同条件の案内が表示される。 | OK | Release描画側へ追加後、共通関数化済み。現状は簡易手動確認ベース。 |
| T28 | 床テクスチャタイル描画 | ゲーム開始後、床描画と `床タイルサイズ` 調整を確認。 | `jimen.png` のタイル敷きで描画され、ステージ端の余りも破綻しない。 | OK | 3/2改修の手動確認結果を反映。 |
| T29 | 難易度選択導線 | タイトル開始時に `Easy / Normal / Hard / Back` を選択し、ゲーム開始/戻るを確認。 | 難易度選択を挟んでゲーム開始でき、`Back` でタイトル選択へ戻れる。 | OK | 3/2改修の手動確認結果を反映。 |
| T30 | 難易度画像UI操作 | 難易度選択画面でキー操作、マウスホバー、左クリックを確認。 | 画像ボタンが拡大表示され、キー/マウスの両方で決定できる。 | OK | 3/2改修の手動確認結果を反映。 |
| T31 | ボス踏みつけ調整の簡易確認 | ボス踏みつけを発生させ、連続テンポ・着地SE・着地演出を確認。 | 初回/連続の予兆テンポ差分が反映され、着地時に `drop.mp3` と演出が出る。 | OK | 現状は簡易確認のみ。詳細なボス戦仕上げ確認は別途実施予定。 |

## 追加ケース（2026-03-11 / ボス被弾手応え・スキル拡張・デバッグ追加）
| ID | 観点 | 手順（要約） | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|
| T32 | ボス被弾時のみヒットストップ/揺れ | ボス攻撃を通常被弾、回避、被弾無敵中でそれぞれ確認。 | 実際にダメージが入った時だけ専用ヒットストップと画面揺れが発火する。 | OK | 3/11実装分の確認結果を反映。 |
| T33 | プレイヤー被弾Star演出 | ボス攻撃または敵攻撃でプレイヤー被弾を確認。 | 床面Starに加え、上半身付近へビルボードStarが複数表示される。 | OK | 3/11実装分の確認結果を反映。 |
| T34 | 実行時HP直接編集 | F1デバッグUIからプレイヤーHP、ボス戦中はボスHPも変更。 | UIで変更した値が実体へ反映され、HUD/挙動も追従する。 | OK | プレイヤー/ボス両方で確認済み。 |
| T35 | スキル取得と保存/読込 | 勝利報酬でスキル取得後、Q/E装備状態のまま保存→再起動→読込。 | 未取得スキルのみ候補に出て、取得済みスキルはスロットと強化段階を保持する。 | OK | 3/11実装分の確認結果を反映。 |
| T36 | スキル挙動（遠距離/近接/衛星） | 3スキルを発動し、敵とボスへヒットさせる。 | 各スキルがクールタイム・ゲージ・ダメージ反映込みで動作し、敵/ボス両方へダメージする。 | OK | 3/11実装分の確認結果を反映。 |
| T37 | 二段階報酬フロー | ステージクリア後、ステータス強化フェーズとスキル獲得/強化フェーズを順に進める。 | 結果画面に留まったまま次フェーズへ進み、候補なし時のみ進行案内へ切り替わる。 | OK | 3/11実装分の確認結果を反映。 |
| T38 | カーソルRayホバー判定 | 3Dオブジェクト、床、各種ゲージUIにカーソルを合わせる。 | F1デバッグUIのホバー対象表示が対象名に応じて切り替わる。 | OK | 3/11実装分の確認結果を反映。 |

## 直近ビルド確認
- 2026-03-11: `Debug|x64` ビルド成功（確認実施）。
- 2026-03-03: `Debug|x64` ビルド成功（警告0 / エラー0）。
- 2026-03-02: `Release|x64` ビルド成功（警告0 / エラー0）。
- 2026-03-02: `Debug|x64` ビルド成功（警告0 / エラー0）。
- 2026-02-27: `Debug|x64` ビルド成功（警告0 / エラー0）。
- 2026-02-15: `Debug|x64` ビルド成功（エラー0）。
- 2026-02-14: `Debug|x64` ビルド成功（エラー0）。
