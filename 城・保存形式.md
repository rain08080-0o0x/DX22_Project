# 城作成エディタ 保存形式メモ

最終更新: 2026-03-12

## 目的
- `SceneCastleEditor` の配置データを保存 / 読込できるようにする
- 将来の `BuildMap` 化、アセット追加、テスト再生に耐えられる形式にする
- 既存の `Transfer` 保存処理と合わせて、`key=value` 形式で実装コストを下げる

## 結論
- 保存形式は `key=value` のテキスト形式を採用する
- ファイル種別名は `CastleSaveData`
- バージョン番号を持たせる
- 配置データは `assetIndex` ではなく `assetId` を保存する
- v1 では「配置情報」と「最低限のエディタ状態」だけを保存する
- Undo / Redo 履歴、サムネイル、描画キャッシュは保存しない

## この形式を選ぶ理由
- 既存の `Transfer.cpp` が `key=value` 形式で保存 / 読込しているため流用しやすい
- JSON を新規導入しなくてよい
- 目視確認と手修正がしやすい
- 学内制作・デバッグ用途では十分

## `assetIndex` を保存しない理由
- 現在の `SceneCastleEditor` は `PlacementInfo.assetIndex` を持っている
- ただし `assetIndex` はアセット順変更で壊れる
- 将来アセット追加や並び替えが入ると互換性が崩れる
- そのため保存時は安定IDである `assetId` を使う

## v1 で保存する項目

### 必須
- `format`
- `version`
- `gridSize`
- `gridHalfExtent`
- `gridHeightStep`
- `placementCount`
- `placement.{i}.assetId`
- `placement.{i}.gridX`
- `placement.{i}.gridY`
- `placement.{i}.gridZ`
- `placement.{i}.rotationQuarterTurns`

### 任意だが v1 で保存してよい項目
- `activeLayer`
- `cameraEyeX`
- `cameraEyeY`
- `cameraEyeZ`
- `cameraLookX`
- `cameraLookY`
- `cameraLookZ`
- `previewRotationQuarterTurns`

### 保存しない項目
- `selectedAssetIndex`
- `selectedPlacementIndex`
- `undoStack`
- `redoStack`
- `thumbnailRT`
- `thumbnailDS`
- `thumbnailDirty`
- `modelView` の一時状態

## v1 の想定ファイル例

```txt
# DX22 castle save
format=CastleSaveData
version=1
gridSize=1.0
gridHalfExtent=16
gridHeightStep=1.0
activeLayer=0
cameraEyeX=8.0
cameraEyeY=10.0
cameraEyeZ=-8.0
cameraLookX=0.0
cameraLookY=0.0
cameraLookZ=0.0
previewRotationQuarterTurns=0
placementCount=3
placement.0.assetId=brick
placement.0.gridX=0
placement.0.gridY=0
placement.0.gridZ=0
placement.0.rotationQuarterTurns=0
placement.1.assetId=wall
placement.1.gridX=1
placement.1.gridY=0
placement.1.gridZ=0
placement.1.rotationQuarterTurns=1
placement.2.assetId=brick
placement.2.gridX=1
placement.2.gridY=1
placement.2.gridZ=0
placement.2.rotationQuarterTurns=0
```

## `assetId` 仕様

### v1 で使うID
- `brick`
- `wall`

### ルール
- 小文字 ASCII
- 保存データでは固定文字列として扱う
- 表示名やモデルパスとは分離する

### 次タスクで必要な変更
- `SceneCastleEditor::AssetInfo` に `assetId` を追加する
- 読込時は `assetId -> assetIndex` の変換テーブルを作る

## 読込ルール
- `format=CastleSaveData` でなければ失敗
- `version=1` のみまず対応
- `placementCount` 件ぶんのキーが揃っていない場合は失敗
- `assetId` が未登録ならその配置は読込失敗扱い
- `rotationQuarterTurns` は `0..3` に正規化
- `gridX / gridY / gridZ` は範囲外なら失敗またはスキップ
- 同一セル重複がある場合は後勝ちではなく失敗に寄せる

## 保存ルール
- まずヘッダを書く
- その後にエディタ状態を書く
- 最後に `placementCount` と配置一覧を書く
- 出力順は固定する
- 並び順は `m_placements` 順を使う

## v1 の責務範囲
- エディタの配置状態を復元できること
- テスト再生で同じ構造を使い回せること

## v1 でやらないこと
- 複数ファイル参照
- ブループリントのネスト
- パーツごとのHPや耐久保存
- ミラー情報や選択状態保存
- バージョン変換処理の自動移行

## 将来拡張で増えそうな項目
- `castleCoreGridX / Y / Z`
- `buildCostTotal`
- `pieceHp`
- `pieceFlags`
- `blueprintName`
- `author`
- `createdAt`
- `updatedAt`

## 実装時の方針
- まずは `v1` を最小構成で通す
- パスや表示名は保存しない
- 読込後に `assetId` からランタイム側のアセットを解決する
- 保存失敗時は既存エディタ状態を壊さない

## このタスクの完了条件
- 保存形式が文書として固定されている
- `assetId` 採用方針が決まっている
- 必須項目と非保存項目が決まっている
- 次タスクでそのまま `CastleSaveData` 実装に入れる
