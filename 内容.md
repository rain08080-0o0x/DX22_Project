• C:\HAL\DX22\プログラム\DX22_Project - コピー\DX22_Project\SceneGame.cpp は、チンチロ風のターン制ダイス勝負の進行・役判
  定・賭け金処理・UI更新をまとめているシーン実装です。プレイヤー/敵のターンごとに「ベット→ロール→役判定→勝敗・配当→次
  ターン」を回します。

  Flow

  - ターン開始 BeginTurn で状態初期化、役表示/ターン表示UI更新、敵ターンなら待機秒数セット。
  - Update は OnlyDice=true なので常に UpdateDiceMode が走る（UpdatePlayerMode は空）。
  - プレイヤーターンは HandleDiceStop → HandlePlayerAutoBet → HandleBetInput → HandleRollInput の順で処理。敵ターンは
    UpdateEnemyTurn が自動ロールする。
  - ダイス停止時 HandleDiceStop が CalcRole で役判定し、スコア加算・役UI更新・賭け結果の確定を行う。

  Role判定

  - CalcRole は3個をソート後に判定。
  - 役とスコア:
      - ピンゾロ(1,1,1) +100
      - ヒフミ(1,2,3) -20
      - 目(2個同じ) +残り1個の値
      - 役なし +0

  賭け処理

  - ベットは m_money からロール時に前払い。
  - ApplyBetResult で配当倍率を計算（ピンゾロ10x、ゾロ目3x、シゴロ2x、目2x、ヒフミ即負け、役なしは残ロールありなら続
    行）。
  - HandlePlayerAutoBet が WaitingBet の間は自動で最低ベット5を入れる（このため手動ベット入力より先に確定されやすい）。

  入力・UI

  - 1/2 でベット(5/10)、Rでロール、右Shiftで役一覧パネル開閉、Escでタイトルへ戻る。
  - 役表示UI・ターン表示UI・所持金UI・スコアUI・役一覧パネル（右からスライド）を描画。
  その他

  - EndTurn はHP減算と次ターン開始の処理があるが、現状このファイル内では呼ばれていません。
  - カメラ・描画行列の設定や Geometory/Sprite のビュー設定を Draw でまとめて行っています。
- - -
## SceneGameの全体像（C:\HAL\DX22\プログラム\DX22_Project - コピー\DX22_Project\SceneGame.h を反映）を整理しました。
  SceneGame.cpp での挙動と対応づけています。

  クラスの役割と大枠

  - SceneGame はダイス（チンチロ）を使ったターン制バトルシーン。OnlyDice=true なので現状はダイスモードのみ動作。
  - プレイヤー/敵のターン制 (TurnOwner, TurnPhase) を持つが、HPやダメージ計算はまだフローに十分組み込まれていない
    （EndTurn が使われていない）。

  役・ベット関連

  - RoleType / RoleResult は出目判定とスコア加算用。
  - BetState はベット待ち→ロール待ち→ロール中→結果確定の4状態。
  - m_money/m_bet/m_rollUsed/m_betState が賭けの中心状態。
  - m_pMoneyUI が所持金表示、ScoreLite がスコア表示。

  ターン制関連

  - BeginTurn/EndTurn がターン切替の責務。
  - m_turnOwner/m_turnPhase が現在ターン管理。
  - m_playerHP/m_enemyHP、m_damageThisTurn、m_resultReady、m_cachedRole があるが、SceneGame.cpp ではまだ活用が限定的。

  - m_pRoleUI … 役表示 (Role_***.png)
  - m_pTurnUI … プレイヤー/敵のターン表示
  - m_role … 役一覧パネル（スライド表示）
  - m_roleListOpen/m_roleListX/TargetX/Speed … パネルのアニメ制御
  - m_pYukari … 役に応じた表情切替キャラ

  入力と状態遷移（cppとの対応）

  - HandlePlayerAutoBet … 自動最低ベット
  - HandleBetInput … 1/2で手動ベット
  - HandleRollInput … Rでロール（ベット先払い）
  - HandleDiceStop … 出目確定 → 役判定 → UI/スコア更新 → ApplyBetResult
  - UpdateEnemyTurn … 敵側は待ち時間後に自動ロール

  現状の注意点

  - EndTurn() でHPを減らす処理はあるが、SceneGame.cpp では呼ばれていないので実質HPバトルが進まない。
  - TurnPhase::Resolve も定義されているが、cpp側では使われていない。
  - HandlePlayerAutoBet が WaitingBet に入った瞬間に最小ベットを確定してしまうため、手動ベットは実質使えない場面が多い。

  必要なら、このまま「ターン制バトルとしてHPダメージまで通る流れ」を設計し直す案や、HandlePlayerAutoBet の仕様を調整する
  案も提案できます。