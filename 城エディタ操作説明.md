# 城エディタ操作説明

## 概要
- この文書は、現在の `SceneCastleEditor` 実装を基準にした操作説明です。
- 城エディタは、3D グリッド上にパーツを配置し、選択、削除、回転、保存、読込を行うための編集シーンです。
- 画面は主に `パレット`、`配置一覧`、`インスペクタ`、`カメラ`、`シーンビュー` で構成されています。

## 画面構成

### パレット
- `Select` タイル、配置アセットのタイル、プレビュー回転ボタン、ツール切替がまとまったウィンドウです。
- アセットを選ぶと配置系ツールへ移れます。
- `Select` タイルを押すと選択系ツールへ戻れます。
- `回転 -90` / `回転 +90` は、これから置くプレビューの向きを 90 度単位で変更します。

### 配置一覧
- 置かれているオブジェクトを一覧表示します。
- 一覧からも選択できます。
- `Ctrl+LeftMouseButton` で選択解除/追加解除、`Shift+LeftMouseButton` で追加選択できます。
- `選択解除` ボタンで現在の選択をすべて外します。

### インスペクタ
- `Undo`、`Redo`、`保存`、`読込` を行えます。
- 単体選択時は `Grid X / Y / Z` と `回転(90度単位)` を直接編集できます。
- 複数選択時は件数表示になり、一括削除のみできます。
- 下部の `ModelView` では選択アセットまたは選択中オブジェクトのモデルを確認できます。

### カメラ
- カメラの `Eye` と `Look` を直接編集できます。
- `カメラをリセット` で初期位置へ戻します。
- `アクティブレイヤー` は、レイヤースナップや `Select Fill` の作業面に使います。

### シーンビュー
- 実際に配置、選択、範囲選択、塗り配置を行うメイン画面です。
- ツールに応じて `LeftMouseButton` の意味が変わります。
- `RightMouseButton Drag`、`MiddleMouseButton Drag`、`MouseWheel` でカメラ操作を行います。

## 基本操作

### シーンビューのカメラ操作
- `RightMouseButton Drag`: カメラ回転
- `MiddleMouseButton Drag`: カメラ平行移動
- `MouseWheel`: ズーム
- `Shift+MouseWheel`: アクティブレイヤー変更

### 共通ショートカット
- `Delete`: 選択中オブジェクトを削除
- `Ctrl+Z`: Undo
- `Ctrl+Y`: Redo
- `Ctrl+Shift+Z`: Redo
- `R`: 単体選択中オブジェクトを `+90 度` 回転
- `Shift+R`: 単体選択中オブジェクトを `-90 度` 回転

## ツール別操作

### Select
- `LeftMouseButton`: クリックした配置物を単体選択
- `Ctrl+LeftMouseButton`: クリックした配置物を選択解除または追加解除
- `Shift+LeftMouseButton`: クリックした配置物を追加選択
- 何もない場所を `LeftMouseButton` で押した場合は選択解除します。

### Select Fill
- `LeftMouseButton`: 始点を設定
- `LeftMouseButton`: 終点を設定して、始点から終点までの 3D 直方体範囲に含まれる配置物を選択
- `Ctrl+LeftMouseButton` で終点確定: 既存の選択へ追加
- 空セルからでも始点、終点を取れます。
- `Shift` を押している間は、配置物ではなくアクティブレイヤー面にスナップして範囲を決めます。
- 始点設定後は、終点候補までの範囲がセル枠でプレビュー表示されます。

### Place
- 配置したいアセットを選んだ状態で使う基本配置モードです。
- `LeftMouseButton`: プレビュー位置に 1 個配置
- 通常は、マウスで指している既存オブジェクトの面の外側に隣接するセルへ配置します。
- 何もヒットしない場合は、地面 `y=0` を基準に配置します。
- `Shift` を押している間は、面基準ではなくアクティブレイヤー面へスナップして配置位置を決めます。

### Paint
- `LeftMouseButton Drag`: ドラッグ中に連続配置
- ドラッグ 1 回分は 1 つの Undo としてまとめて戻せます。
- 連続配置中も、既に埋まっているセルには置けません。

### Fill
- `LeftMouseButton`: 始点を設定
- `LeftMouseButton`: 終点を設定して範囲配置
- 始点設定後の `Shift+MouseWheel`: 始点からの高さオフセットを変更
- Fill は 3D 直方体埋めではなく、`XZ`、`XY`、`YZ` のいずれか 1 平面上の範囲だけを確定できます。
- つまり、始点と終点は少なくとも 1 軸が一致している必要があります。
- 始点設定後は、終点候補までの配置プレビューが表示されます。

## 選択後の編集

### 単体選択
- インスペクタで座標と回転を直接変更できます。
- `R / Shift+R` の回転ショートカットも使えます。
- `削除` ボタンでも削除できます。

### 複数選択
- インスペクタでは `N件選択中` 表示になり、位置編集と回転編集はできません。
- `選択中を一括削除` または `Delete` でまとめて削除できます。

## 保存と読込
- `インスペクタ` の `保存` ボタンで現在の配置を保存します。
- `読込` ボタンで保存済みデータを読み込みます。
- 既定の保存先は `Assets/castle_editor_save.cfg` です。
- 読込時は形式やアセット ID、範囲、重複などを検証し、失敗時は既存状態を壊さないようになっています。

## ModelView
- `RightMouseButton Drag`: モデル回転
- `MouseWheel`: ズーム
- `ModelViewリセット`: モデルビュー用カメラを初期状態へ戻す

## 補足
- 配置できる範囲は、おおむね `X/Z = -16 ～ 16`、`Y = 0 ～ 16` のグリッドです。
- 配置先が範囲外、または既に埋まっているセルの場合は配置できません。
- ツール切替やアセット切替を行うと、進行中の `Paint`、`Fill`、`Select Fill` の始点状態はリセットされます。
- アセットを選んだ直後、現在が選択系ツールなら自動で `Place` に切り替わります。
