# 作業レポート 2026-03-17

## 概要
アクションゲーム側に、デバッグ経由で任意タイミングから開始できる `挑戦ステージ` の仮実装を追加した。
今回はマップ分岐への本組み込みではなく、`ノーダメ挑戦` と `防衛挑戦` の 2 種を先行で動かし、成功 / 失敗に応じた報酬段階まで通した。

## 今回の実装内容

### 1. 挑戦ステージのデバッグ起動導線
- `F1` のデバッグ UI に `挑戦` タブを追加した
- `ノーダメ挑戦を開始` と `防衛挑戦を開始` のボタンから、任意のタイミングで `SceneGame` に入れるようにした
- 挑戦報酬の受取後は、マップ選択ではなくゲームシーンへ戻るデバッグ導線を追加した

### 2. ノーダメ挑戦の実装
- 敵を配置せず、地面に予兆を出した後で攻撃判定が発生する耐久型ステージを追加した
- 予兆円の半径、発生間隔、予兆時間、同時発生数、挑戦全体の制限時間を調整値として持たせた
- 被弾時は挑戦失敗にしつつ、最低 1 個の報酬がもらえる流れへつないだ
- ダメージは即死ではなく低めの固定値とし、被弾回数も記録するようにした

### 3. 防衛挑戦の実装
- ステージ中央に防衛対象ビーコンを置き、端から出現した敵がビーコンへ向かう形式を追加した
- 防衛対象の HP、半径、敵接触時ダメージ、出現間隔、制限時間、同時出現上限を調整値化した
- 敵はプレイヤーよりビーコンを優先して追うようにし、防衛挑戦中は強制追跡状態で動くようにした
- 敵移動速度は鈍足寄りにし、HP は敵種別ごとに上乗せ倍率を持たせた

### 4. 挑戦専用の報酬段階
- 挑戦用に `ステータス / スキル強化` の混合報酬フェーズを追加した
- 成功時は最大 3 個、失敗時でも最低 1 個の報酬を受け取れるようにした
- 現状の段階は `早期失敗=1個 / 後半失敗=2個 / 成功=3個` の扱いにしている
- 報酬 UI の文言も挑戦用表示へ対応させた

### 5. ImGui からの挑戦調整
- `挑戦` タブに現在の挑戦種別、経過時間、残り報酬段階、被弾回数、ビーコン HP を表示するようにした
- `ノーダメ挑戦` と `防衛挑戦` の主要パラメータを ImGui から直接編集できるようにした
- 調整値は `Assets/gameplay_tuning.cfg` に保存され、次回起動時にも保持される

## 現在の到達点
- デバッグ経由で、`ノーダメ挑戦` と `防衛挑戦` を個別に起動して検証できる状態になった
- 挑戦成功 / 失敗に応じた報酬数の切替と、報酬受取後のゲーム復帰までつながった
- 今後はこの仕組みをベースに、`2 / 3 / 5 / 6` ステージのランダム分岐へ広げられる状態になった

## 今後の拡張候補
1. 挑戦ステージのマップ分岐組み込み
2. ノーダメ挑戦の攻撃パターン追加
3. 防衛ビーコンの専用 UI 表示
4. 挑戦失敗段階の報酬ルール再調整
5. `挑戦 / 戦闘` の出現比率調整

## ビルド確認
- `Debug|x64`
- `Release|x64`
- 結果: 2026-03-17 時点でビルド成功、警告 0、エラー 0
